group|tag|experiment_id|status|checkpoint|priority|min_runtime|override|results_dataset|workspace|cluster

# Rows are not hand-written, but this file is hand-edited. experiment_id is a
# Beaker id that exists only after submission, and the job runner appends one
# row per launched job (see append_to_experiments_file in job_runner/lib.sh,
# called from job_runner/uncoupled_train.sh and job_runner/uncoupled_finetune.sh).
# A hand-written placeholder row would be duplicated by that append. Editing an
# existing row by hand is the normal way to drive downstream jobs; see below.

# Rows will arrive under two groups:
#   cm4_1pct_46to125_piC_156to235-ocean_baseline       (pretraining)
#   cm4_1pct_46to125_piC_156to235-ocean_baseline-ft    (finetuning)

# The runner writes them with status "training". The status word is then the
# hand-edited control for what runs next, and it selects the config by name:
# job_runner/evaluate.sh acts on ^run_ (excluding ^run_inf_) and reads the tag
# off the suffix, so status run_ICx1 runs evaluator-config-ICx1.yaml from this
# directory; job_runner/inference.sh acts on ^run_inf_ the same way. Any other
# word is inert to both, which is how a row is parked (see the reference
# directory's "invalid" rows).

cm4_1pct_46to125_piC_156to235-ocean_baseline|rs0|01M01AXAZFFPD5AVKF4BD1MB9P|trained|best_inference_ckpt|normal|--min-runtime 8h

cm4_1pct_46to125_piC_156to235-ocean_baseline|rs1|01M01AXQP8Q3MKMVEBKH6MFSKX|trained|best_inference_ckpt|normal|--min-runtime 8h

cm4_1pct_46to125_piC_156to235-ocean_baseline|rs2|01M01AY4W5WVQ6XX3VZJQBVT9E|trained|best_inference_ckpt|normal|--min-runtime 8h

cm4_1pct_46to125_piC_156to235-ocean_baseline-ft|rs0|01M06TXEMBTJNS6085BE9BW89H|ran_piC-ic0151-200yr|best_inference_ckpt|high|--min-runtime 3h|data_writer.save_monthly_files=false||ai2/ace|jupiter
cm4_1pct_46to125_piC_156to235-ocean_baseline-ft|rs1|01M08JR4AQE9MGJM3H55KK67W4|ran_piC-ic0151-200yr|best_inference_ckpt|high|--min-runtime 3h|data_writer.save_monthly_files=false||ai2/ace|jupiter
cm4_1pct_46to125_piC_156to235-ocean_baseline-ft|rs2|01M08JRM77J2RHQZT125JGB4Z4|ran_piC-ic0151-200yr|best_inference_ckpt|high|--min-runtime 3h|data_writer.save_monthly_files=false||ai2/ace|jupiter

# The three -ft rows above are set to the 200-yr piControl rollout as the
# lightest rung of a corrector ablation: this arm's finetune template strips the
# stepper block and merges the pretraining stepper, so -ft inherits a corrector
# with NO ocean_heat_content_correction and NO surface_energy_flux_correction --
# only force_positive_names and the sea-ice fraction correction. Compared
# against ocean_wide (no OHC correction, prescribed surface energy flux) and
# ocean_wide_ohc_clip (both), which carry the same rollout.

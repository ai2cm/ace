# Masked-input ablation on the ocean_precorr_clip substrate: a 2x2 of
# stepper.input_masking fill_value x masking scope. train-config.yaml here IS
# ocean_precorr_clip (ocean_precorr + max_grad_norm 1.0), so the already-running
# cm4_1pct_46to125_piC_156to235-ocean_precorr_clip rs0/1/2 are the fourth cell
# and need no row here.
#
#                  | current exclude      | maskall
# -----------------+----------------------+-----------------------------------
# fill_value: 0.0  | ocean_precorr_clip   | ocean_precorr_clip_maskall
#                  | (running, rs0/1/2)   |
# fill_value: mean | ..._clip_fillmean    | ..._clip_fillmean_maskall
#
# "maskall" = mask every input except the two that are supposed to carry the
# mask location, i.e. exclude_names_and_prefixes drops the ten atmosphere
# forcings and adds sea_surface_fraction.
#
# SEEDING. rs0 launches; rs1/rs2 are staged at `skip`. One seed is a screen,
# not a measurement: across-seed CV in this protocol is 0.2-0.9% on
# val_1pct_late and 3-5% on the inference nrmse, but 14-29% on so_0. So a
# single seed can only read a so_0 effect of roughly 40-60%+ (the 1deg maskall
# arm's was ~60%, hence the screen), and a so_0 NULL at one seed means nothing.
# Read the arms at ~epoch 33, where the existing arms already separate, against
# the 3-seed ocean_precorr_clip range. The fillmean_maskall-vs-maskall
# comparison in particular is a difference-of-differences and cannot be settled
# at one seed each.
#
# To promote an arm to 3 seeds: first flip its rs0 `train` -> `training`, THEN
# flip its rs1/rs2 `skip` -> `train`. The runner only reads this file and never
# writes it back, so an rs0 row left at `train` relaunches rs0 alongside the new
# seeds.

group|tag|skip_or_train|priority|cluster|n_gpus|shared_mem|retries|workspace|override_args|min_runtime

cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean|rs0|training|high|jupiter|4|500GiB|3|ai2/ace|seed=0 stepper.input_masking.fill_value=mean
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean|rs1|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=1 stepper.input_masking.fill_value=mean
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean|rs2|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=2 stepper.input_masking.fill_value=mean

cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskall|rs0|training|high|jupiter|4|500GiB|3|ai2/ace|seed=0 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction]'
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskall|rs1|training|high|jupiter|4|500GiB|3|ai2/ace|seed=1 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction]'
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskall|rs2|training|high|jupiter|4|500GiB|3|ai2/ace|seed=2 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction]'

cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_maskall|rs0|training|high|jupiter|4|500GiB|3|ai2/ace|seed=0 stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction]'
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_maskall|rs1|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=1 stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction]'
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_maskall|rs2|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=2 stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction]'

# ARM B -- decomposing the maskall change over the fillmean side, added
# 2026-08-21. The maskall cell above moves two things at once: the ten
# atmosphere forcings become masked AND sea_surface_fraction becomes unmasked.
# These two rows split that, and split the forcings by what they carry:
#
#   cell                    exclude list                       masked
#   ..._fillmean            land_fraction + 10 forcings         86/97
#   ..._fillmean_maskfw     ^ minus LHTFLsfc, PRATEsfc,          89/97
#                            total_frozen_precipitation_rate
#   ..._fillmean_maskatm    [land_fraction]                     96/97
#   ..._fillmean_maskall    [land_fraction, sea_surface_fraction] 95/97
#
# so (maskfw - fillmean) = the freshwater channels, (maskatm - maskfw) =
# radiation + wind stress, (maskall - maskatm) = the sea_surface_fraction flip.
# The hypothesis under test is that the freshwater trio carries most of the
# so_0 effect: surface salinity is the one field whose forcing the network must
# infer from the atmosphere channels and whose budget no corrector pins, so
# land-side evaporation and precipitation are the values it should not be able
# to read across the mask edge.
#
# Same seeding caveat as above: one seed reads only a ~40-60% so_0 effect, and
# a decomposition splits the effect, so only "the freshwater trio reproduces
# most of maskall" is readable at rs0. rs1/rs2 staged at skip.

cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskfw|rs0|training|high|jupiter|4|500GiB|3|ai2/ace|seed=0 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,DLWRFsfc,DSWRFsfc,ULWRFsfc,USWRFsfc,SHTFLsfc,eastward_surface_wind_stress,northward_surface_wind_stress]'
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskfw|rs1|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=1 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,DLWRFsfc,DSWRFsfc,ULWRFsfc,USWRFsfc,SHTFLsfc,eastward_surface_wind_stress,northward_surface_wind_stress]'
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskfw|rs2|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=2 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,DLWRFsfc,DSWRFsfc,ULWRFsfc,USWRFsfc,SHTFLsfc,eastward_surface_wind_stress,northward_surface_wind_stress]'

cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskatm|rs0|training|high|jupiter|4|500GiB|3|ai2/ace|seed=0 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction]'
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskatm|rs1|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=1 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction]'
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskatm|rs2|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=2 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction]'

# ARM C -- the freshwater test, run without the confound, added 2026-08-24.
#
# The 130-yr and 200-yr standalone rollouts said the maskall win was never about
# completing the mask: every arm that collapses is one that MEAN-FILLS
# sea_surface_fraction, and the only mean-fill arm that survives both scenarios
# is the one that excludes it. maskall vs maskatm differ in exactly that channel
# and differ in whether they collapse; masked count does not order the collapses
# at all (86 -> yr 97, 89 -> yr 29, 96 -> yr 47, 95 -> never).
#
# Why that channel specifically: sea_surface_fraction is geometry, not state --
# its value IS the land/ocean distinction. Mean-fill puts its land cells at the
# normalized value 0, indistinguishable from a real half-ocean coastal cell,
# where fill_value 0.0 puts them far outside the field's range. Which is also
# why both fill_value 0.0 arms are immune regardless of masking scope.
#
# So maskfw inherited a broken sea_surface_fraction from the base exclude list,
# and the freshwater hypothesis has never actually been tested. maskfw had the
# best short-horizon so_0 of any arm (0.111 at its selected epoch) and was the
# FIRST to collapse (rollout yr 29 / 57). This row is maskfw with
# sea_surface_fraction added back to the exclude list -- ONE channel different
# from a known collapse, 88/97 masked against maskfw's 89/97:
#
#   cell                        exclude list                          masked
#   ..._fillmean_maskfw         land_fraction + radiation + wind       89/97
#   ..._fillmean_maskfw_keepssf ^ plus sea_surface_fraction            88/97
#
# Read it as: if this does not collapse, sea_surface_fraction is the driver and
# the freshwater masking is recovered along with it -- which would make this the
# best arm in the protocol on surface salinity. If it still collapses, the
# freshwater masking itself is implicated and the sea_surface_fraction story is
# incomplete.
#
# Same seeding caveat as above: rs0 is a screen. But the readout here is a
# collapse/no-collapse over a 130-200 yr rollout, not a so_0 number, and no seed
# of any arm has ever half-collapsed -- so one seed does carry the qualitative
# result, while the onset year (which does not even reproduce across scenarios)
# does not.

cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskfw_keepssf|rs0|training|high|jupiter|4|500GiB|3|ai2/ace|seed=0 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction,DLWRFsfc,DSWRFsfc,ULWRFsfc,USWRFsfc,SHTFLsfc,eastward_surface_wind_stress,northward_surface_wind_stress]'
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskfw_keepssf|rs1|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=1 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction,DLWRFsfc,DSWRFsfc,ULWRFsfc,USWRFsfc,SHTFLsfc,eastward_surface_wind_stress,northward_surface_wind_stress]'
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskfw_keepssf|rs2|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=2 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction,DLWRFsfc,DSWRFsfc,ULWRFsfc,USWRFsfc,SHTFLsfc,eastward_surface_wind_stress,northward_surface_wind_stress]'

# SEEDS PROMOTED 2026-08-24: ..._fillmean_maskall rs1/rs2 -> train.
#
# This is the arm the verdict rests on and it was a single seed. Across the
# 130-yr 1pctCO2 and 200-yr piControl standalone rollouts it is the only
# mean-fill arm that never loses surface salinity, and it beats every control
# seed on it -- but "never collapses" at n=1 is exactly the claim a second and
# third seed are needed for, since three sibling arms DO collapse (rollout yr
# 29/57, 47/166, 97/75). Promote, then run both standalone rollouts against each
# new seed; the readout is collapse/no-collapse, not a so_0 number.
#
# rs0 stays at `training` so the runner does not relaunch it.

# ARM D -- per-channel fill value. STAGED AT `skip`, NOT LAUNCHED.
#
# Requires ace PR #1458 (feature/per-channel-mask-fill-value), which is on this
# branch as a cherry-pick and is NOT on main. If that PR does not merge, this arm
# cannot run on mainline code. Nothing here touches
# exper/2026-08-18-precorr-optim, which this branch was cut from.
#
# WHY. The standalone rollouts split cleanly by field: mean-fill wins every 2D
# surface field, zero-fill wins the 19-level velocity and temperature profiles by
# 1.2-1.4x. Both directions are real and a single global fill_value cannot express
# them at once. The mechanism is that a masked cell means two different things --
# for sst or salinity it means "no data here", where the channel mean is a neutral
# filler and 0.0 injects a large out-of-range value near coastlines; for a
# depth-resolved channel below the sea floor it means "genuinely dry", which is
# usable information that only an out-of-range marker conveys. Nothing else in the
# inputs states dryness per level: land_fraction is a single 2D field, while the
# wet fraction falls from 0.693 at level 0 to 0.073 at level 18, with 89.4% of
# surface-ocean columns dry by the deepest level.
#
# WHAT. The surviving recipe (fill_value: mean, excluding land_fraction and
# sea_surface_fraction) plus fill_value_overrides sending the four depth-resolved
# families and deptho to 0.0. Resolved and checked against TrainConfig:
#   excluded, never masked   2  land_fraction, sea_surface_fraction
#   fill 0.0                78  deptho, hfgeou + so_/thetao_/uo_/vo_ (4 x 19)
#   fill mean               17  sst, zos, sfdsi, UI, VI, sea ice, forcings
#
# Two judgement calls worth revisiting before launch:
#   - so_0 and thetao_0 are level-0 (surface) but ride the family prefix into
#     zero-fill. mask_0 is bit-identical to mask_2d, so their masked cells are
#     exactly the land cells that sst's are -- the alternative is to add
#     so_0/thetao_0 exact-name entries at "mean". Family uniformity was chosen.
#   - hfgeou is zero-filled, at elynn's call. It is NaN on land like deptho and is
#     a sea-floor boundary field, so it belongs with the depth group rather than
#     the surface set despite being 2D.
#
# READ IT AS: against ..._fillmean_maskall (rs0/1/2), which is identical except
# for the overrides. If the interior profiles improve toward the zero-fill arms
# while the surface fields hold, per-channel fill is worth keeping. If nothing
# moves, the surface/interior split is not about the fill value and the feature
# should not merge on this evidence.

cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskall_zerodeep|rs0|training|high|jupiter|4|500GiB|3|ai2/ace|seed=0 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction]' stepper.input_masking.fill_value_overrides='{"so_": 0.0, "thetao_": 0.0, "uo_": 0.0, "vo_": 0.0, "deptho": 0.0, "hfgeou": 0.0}'
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskall_zerodeep|rs1|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=1 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction]' stepper.input_masking.fill_value_overrides='{"so_": 0.0, "thetao_": 0.0, "uo_": 0.0, "vo_": 0.0, "deptho": 0.0, "hfgeou": 0.0}'
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskall_zerodeep|rs2|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=2 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction]' stepper.input_masking.fill_value_overrides='{"so_": 0.0, "thetao_": 0.0, "uo_": 0.0, "vo_": 0.0, "deptho": 0.0, "hfgeou": 0.0}'

# ARM E -- Troy's physically-motivated fill. STAGED AT `skip`, NOT LAUNCHED.
#
# Arm D with thetao_ dropped from fill_value_overrides, so it falls back to
# fill_value: mean. Requires ace PR #1458 exactly as Arm D does. ONE change from
# Arm D, nothing else moves.
#
# WHY. Troy's reading of what land cells should hold, in the un-normalized space
# the fill is applied in (input_masking.build takes step.normalizer.means, and
# apply_input_process_func runs before the step normalizes -- so a fill value is
# a physical value, not a normalized one):
#   - land velocity is 0 m/s        -> uo_, vo_ at 0.0   (Arm D already does this)
#   - land salinity is 0 psu        -> so_ at 0.0        (Arm D already does this)
#   - land temperature is climatology -> thetao_ at mean (Arm D has 0.0: THE CHANGE)
# sst is already mean-filled in Arm D, so this is also the consistency fix: sst
# (in K) and thetao_0 (in degC) cover a bit-identical mask -- mask_0 IS mask_2d --
# and Arm D fills them differently, mean for one and 0.0 for the other. thetao_0
# is the level-0 surface temperature; sst is the surface temperature. After this
# change all temperature is filled with its own climatology regardless of unit.
#
# WHAT THE EVIDENCE SAYS, AND WHERE IT CUTS AGAINST THIS ARM. At 75 epochs Arm D
# beat 3-seed ..._fillmean_maskall on the 19-level thetao family mean by 1.7x
# (0.0163 vs 0.0277-0.0296 on 1pct time_mean_norm/rmse), so zero-fill measurably
# wins for thetao and this arm gives that up. Two reasons to run it anyway:
#   - The "out-of-range marker" rationale in Arm D's block above does not hold for
#     thetao. It is in degC, range -2.2 to 31.5 at level 0 and -0.15 to 4.33 at
#     level 18, so 0.0 is an ORDINARY in-range ocean temperature -- at depth it is
#     nearly the climatological mean already. Whatever zero-fill buys for thetao,
#     it is not dryness marking, and the two options are physically much closer
#     than the config makes them look.
#   - Arm D's cost landed on sst: it fell back to the fill-0.0 level (1pct
#     time_mean/rmse 0.1203 against 0.0899-0.0929 for mean-fill) even though sst
#     itself is mean-filled in Arm D. The sst/thetao_0 fill inconsistency is the
#     leading candidate for that, and this arm is its direct test.
#
# READ IT AS: against Arm D (thetao_ 0.0) and against ..._fillmean_maskall rs0/1/2
# (everything mean). If sst recovers toward 0.090 while so_/uo_/vo_ hold their
# Arm D gains, the physically-motivated fill is the recipe and the sst regression
# was the inconsistency. If sst stays at 0.120 and thetao degrades to 0.028, the
# interior wants zero-fill on its own terms and Arm D stands.
#
# FLUX SCOPE UNCHANGED, DELIBERATELY. Troy also suggests leaving the flux channels
# unfilled since they are valid over land. That is a second variable and it is
# already being measured separately: Arm C (..._fillmean_maskfw_keepssf, masks
# only the freshwater trio and leaves radiation, sensible heat and wind stress
# unmasked) trained to 75 epochs and its two standalone rollouts launched
# 2026-09-03. This arm keeps maskall's scope -- all ten forcings mean-filled --
# because maskall is the only recipe with a verified 200-yr no-collapse record and
# an arm should fork from that changing one thing. Note the flux intuition is
# already refuted for the freshwater trio specifically: ..._fillmean leaves all
# ten unmasked and its 1pct so_0 was 0.758 against 0.093 for masking just the
# trio.
#
# SEEDING. rs0 only; rs1/rs2 staged at `skip`, same caveat as every arm above --
# one seed reads a ~40-60% so_0 effect and no smaller. The primary readouts here
# (sst, the thetao family mean) have 3-5% seed CV, so they are readable at rs0.

cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskall_zerodeep_meanthetao|rs0|train|high|jupiter|4|500GiB|3|ai2/ace|seed=0 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction]' stepper.input_masking.fill_value_overrides='{"so_": 0.0, "uo_": 0.0, "vo_": 0.0, "deptho": 0.0, "hfgeou": 0.0}'|3h
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskall_zerodeep_meanthetao|rs1|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=1 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction]' stepper.input_masking.fill_value_overrides='{"so_": 0.0, "uo_": 0.0, "vo_": 0.0, "deptho": 0.0, "hfgeou": 0.0}'|3h
cm4_1pct_46to125_piC_156to235-ocean_precorr_clip_fillmean_maskall_zerodeep_meanthetao|rs2|skip|high|jupiter|4|500GiB|3|ai2/ace|seed=2 stepper.input_masking.fill_value=mean stepper.input_masking.exclude_names_and_prefixes='[land_fraction,sea_surface_fraction]' stepper.input_masking.fill_value_overrides='{"so_": 0.0, "uo_": 0.0, "vo_": 0.0, "deptho": 0.0, "hfgeou": 0.0}'|3h

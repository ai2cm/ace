group|tag|wandb_project|wandb_id|ckpt_type|skip_or_train|priority|cluster|n_gpus|shared_mem|retries|workspace|override|results_dataset

# The finetune leg is not staged yet: its wandb_id column must be the pretraining
# runs' ids, which do not exist until the rows in training.txt have run. Add rows
# here (mirroring ocean_baseline/finetuning.txt) after pretraining completes.
# Nothing needs an override here -- the finetune stepper is inherited from the
# pretraining checkpoint, so fill_value=mean, the dropped sea ice variables and
# the absent surface_energy_flux_correction all carry over. In particular, do
# NOT add the surface_energy_flux_correction.method='prescribed' term that the
# other arms' finetune rows will carry: it would fail here, since this arm has
# no ocean_sea_ice_fraction input for it to read.
